/target
/gen

# Sidecar / bundled binaries - not committed to git (run pnpm fetch:ffmpeg / build:funasr-sidecar)
/binaries/funasr-asr*
/binaries/ffmpeg
/binaries/ffmpeg.exe
!/binaries/.gitkeep

# FunASR models - large files, download separately
/models/sensevoice/*
!/models/sensevoice/.gitkeep
